services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_HOST_AUTH_METHOD: trust
      POSTGRES_DB: bfit
    ports:
      - "5432:5432"
    volumes:
      - bfit_pgdata:/var/lib/postgresql/data

volumes:
  bfit_pgdata:
